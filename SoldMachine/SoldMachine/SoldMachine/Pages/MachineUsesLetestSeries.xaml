<?xml version="1.0" encoding="utf-8" ?>
<ContentPage xmlns="http://xamarin.com/schemas/2014/forms"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             x:Class="SoldMachine.Pages.MachineUsesLetestSeries"
             BackgroundImageSource="HelperPage.gfif">
    <ContentPage.Content>
        <StackLayout>

            <!--<Entry Text="{Binding LetestList}"
                    x:Name="demo"
                VerticalOptions="CenterAndExpand" 
                HorizontalOptions="CenterAndExpand" />-->

            <ListView x:Name="demolist"
                VerticalOptions="CenterAndExpand" 
                HorizontalOptions="CenterAndExpand"/>
            <!--<ListView.ItemTemplate>
                    <DataTemplate>
                        <ViewCell>
                            <StackLayout>
                                <Label Text="{Binding xdemo}"/>
                            </StackLayout>
                        </ViewCell>
                    </DataTemplate>
                </ListView.ItemTemplate>-->
          

        </StackLayout>
    </ContentPage.Content>
</ContentPage>